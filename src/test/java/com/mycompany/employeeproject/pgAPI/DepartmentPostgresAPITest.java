package com.mycompany.employeeproject.pgAPI;

import com.mycompany.employeeproject.model.Department;
import java.util.List;
import org.junit.Test;
import static org.junit.Assert.*;

public class DepartmentPostgresAPITest {
/*
    @Test
    public void testReadDepartments() {
        System.out.println("readDepartments");
        DepartmentRepository instance = new DepartmentRepository();
        List<Department> result = instance.readDepartments();
        assertNotNull(result);
        assertTrue(result.size() > 0);
    }

    @Test
    public void testGetDepartmentsByName() {
        System.out.println("getDepartmentsByName");
        String nameDep = "";
        DepartmentRepository instance = new DepartmentRepository();
        Department expResult = new Department(4, "Sails", "");
        Department result = instance.getDepartmentsByName(nameDep);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");
    }

    @Test
    public void testAddDepartment() {
        System.out.println("addDepartment");
        Department dep = null;
        DepartmentRepository instance = new DepartmentRepository();
        int expResult = 0;
        int result = instance.addDepartment(dep);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");
    }

    @Test
    public void testUpdate() {
        System.out.println("update");
        Department dep = null;
        DepartmentRepository instance = new DepartmentRepository();
        boolean expResult = false;
        boolean result = instance.update(dep);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");
    }

    @Test
    public void testDelete() {
        System.out.println("delete");
        int id = 0;
        DepartmentRepository instance = new DepartmentRepository();
        boolean expResult = false;
        boolean result = instance.delete(id);
        assertEquals(expResult, result);
        // TODO review the generated test code and remove the default call to fail.
        fail("The test case is a prototype.");
    }
  */  
}
